<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- 顯示正確比例且透明背景的圖標 -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground_vector</item>
        <item name="android:windowSplashScreenBackground">#121212</item>
        <!-- 強制讓圖標背景跟視窗背景一致，消除白圈 -->
        <item name="android:windowSplashScreenIconBackgroundColor">#121212</item>
        
        <!-- 作為後備背景（針對非 Android 12 的情況） -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">#121212</item>
    </style>
</resources>
